# EPIC-1 EXAMPLES: Core Infrastructure

This EPIC is about building the core infrastructure to ensure the web application runs in a federated independent manner with independent docker containers.

The infrastructure is based on Kubernetes, a lightweight installation of it using Rancher.

The underlying VMs are Linux based on Ubuntu24.

